const crypto=require('crypto');
const db=require('../lib/db');
const ui=require('../lib/ui');
const {categories,esc,attr,fmt,paras,layout,header,pautaModal}=ui;

function query(req){return new URL(req.url,'https://local').searchParams}
function cookies(req){return Object.fromEntries(String(req.headers.cookie||'').split(';').map(x=>x.trim().split('=').map(decodeURIComponent)).filter(x=>x[0]))}
function form(req){return new Promise((resolve,reject)=>{let d='';req.on('data',c=>{d+=c;if(d.length>2_000_000)req.destroy()});req.on('end',()=>resolve(Object.fromEntries(new URLSearchParams(d))));req.on('error',reject)})}
function sig(v){return crypto.createHmac('sha256',process.env.SESSION_SECRET||'').update(v).digest('base64url')}
function token(email){const p=Buffer.from(JSON.stringify({email,exp:Date.now()+43200000})).toString('base64url');return `${p}.${sig(p)}`}
function isAdmin(req){try{const t=cookies(req).ep_session;if(!t)return false;const [p,s]=t.split('.');if(!p||!s)return false;const a=Buffer.from(s),b=Buffer.from(sig(p));if(a.length!==b.length||!crypto.timingSafeEqual(a,b))return false;const d=JSON.parse(Buffer.from(p,'base64url').toString());return d.exp>Date.now()&&d.email===process.env.ADMIN_EMAIL}catch{return false}}
function send(res,html,status=200){res.statusCode=status;res.setHeader('Content-Type','text/html; charset=utf-8');res.setHeader('Cache-Control','no-store');res.end(html)}
function redirect(res,url){res.statusCode=303;res.setHeader('Location',url);res.end()}

async function home(req,res){
 const rows=await db.published();const featured=rows.find(x=>x.featured)||rows[0];
 const cards=rows.map(a=>`<a class="card" data-cat="${attr(a.category)}" data-search="${attr((a.title+' '+a.deck+' '+a.category).toLowerCase())}" href="/materia/${attr(a.slug)}"><img src="${attr(a.cover_url||'/assets/agenda-juventude-2026.svg')}" alt="${attr(a.cover_alt||a.title)}"><div class="copy"><div class="eye">${esc(a.category)}</div><h3>${esc(a.title)}</h3><p>${esc(a.deck)}</p><div class="meta">${esc(a.author)} • ${fmt(a.published_at||a.created_at)}</div></div></a>`).join('');
 const content=`${header()}<main>${featured?`<section class="hero"><div class="shell heroGrid"><a class="feature" href="/materia/${attr(featured.slug)}"><img src="${attr(featured.cover_url)}" alt="${attr(featured.cover_alt||featured.title)}"><span class="shade"></span><div class="featureCopy"><div class="eye light">DESTAQUE • ${esc(featured.category)}</div><h1>${esc(featured.title)}</h1><p>${esc(featured.deck)}</p><div class="meta"><span>${esc(featured.author)}</span><span>•</span><span>${fmt(featured.published_at||featured.created_at)}</span></div></div></a><aside class="radar"><div class="eye">RADAR LOCAL</div><h2>O que está em pauta</h2>${categories.map((c,i)=>`<a href="/#ultimas" onclick="setTimeout(()=>filterCat('${attr(c)}'),50)">0${i+1} ${esc(c)} →</a>`).join('')}</aside></div></section>`:''}<section class="section" id="ultimas"><div class="shell"><div class="head"><div><div class="eye">ÚLTIMAS</div><h2>A cidade em movimento</h2></div><input id="search" class="search" placeholder="Buscar notícia, assunto ou editoria" oninput="filterAll()"></div><div class="chips"><button class="chip active" data-chip="Todos" onclick="filterCat('Todos')">Todos</button>${categories.map(c=>`<button class="chip" data-chip="${attr(c)}" onclick="filterCat('${attr(c)}')">${esc(c)}</button>`).join('')}</div><div class="grid" id="cards">${cards}</div></div></section><section class="section alt" id="editorias"><div class="shell"><div class="eye">EDITORIAS</div><h2>Pessoas no centro da conversa</h2><div class="editorias">${categories.map(c=>`<a class="ed" href="/#ultimas" onclick="setTimeout(()=>filterCat('${attr(c)}'),50)"><strong>${esc(c)}</strong><span>Explorar →</span></a>`).join('')}</div></div></section><section class="section service"><div class="shell serviceGrid"><div><div class="eye light">SERVIÇO</div><h2>Informação que resolve</h2><p>Agenda, comunidade, participação e vídeo em poucos toques.</p></div><div class="serviceBtns"><a href="/#ultimas">Agenda Positiva</a><a href="/#ultimas">Comunidade</a><button onclick="pauta(true)">Envie uma pauta</button><a href="/reels">Em Vídeo</a></div></div></section><section class="section" id="diretriz"><div class="shell"><div class="eye">TRANSPARÊNCIA</div><h2>Jornalismo com processo visível</h2><p>O Em Pauta é independente, plural e não partidário. Pautas do público passam por checagem, contexto e revisão humana antes da publicação.</p></div></section></main><footer class="footer"><div class="shell" style="display:grid;grid-template-columns:minmax(260px,1.15fr) minmax(220px,.8fr) minmax(260px,.9fr);gap:28px;align-items:start"><div id="sobre-fundadora"><img src="/logo.svg" alt="Em Pauta"><p>Conexão, histórias e propósito.</p><div style="margin-top:14px;padding:14px 0;border-top:1px solid #ffffff24"><div style="display:flex;align-items:center;gap:10px"><span aria-hidden="true" style="display:grid;place-items:center;width:42px;height:42px;border:1px solid #D49A23;border-radius:50%;color:#D49A23;font-size:20px">♕</span><div><strong style="display:block;color:#fff">Fundado por Fabiana Rizzo</strong><small style="color:#d7deea">Fundadora do Em Pauta</small></div></div><a href="#sobre-fundadora" aria-label="Sobre Fabiana Rizzo, fundadora do Em Pauta" style="display:inline-flex;align-items:center;gap:8px;margin-top:12px;border:1px solid #D49A23;border-radius:999px;padding:9px 13px;color:#fff;font-weight:700;font-size:12px"><span aria-hidden="true">👤</span> Sobre a fundadora</a></div><p style="font-size:12px;line-height:1.7;margin-top:14px">Responsável editorial: Fabiana Rizzo<br>Avaré, São Paulo<br><a href="mailto:fabianacristinarizzo@gmail.com" style="text-decoration:underline">fabianacristinarizzo@gmail.com</a></p></div><div><div class="eye light" style="color:#D49A23;margin-bottom:12px">INSTITUCIONAL</div><nav aria-label="Links institucionais" style="display:grid;gap:10px;font-size:13px"><a href="/#diretriz">Sobre o Em Pauta</a><a href="#sobre-fundadora">Meu trabalho</a><a href="/#diretriz">Diretriz editorial</a><a href="/#diretriz">Política editorial</a><a href="/reels">Em Vídeo</a><a href="/#diretriz">Acessibilidade</a><a href="mailto:fabianacristinarizzo@gmail.com">Contato e parcerias</a></nav></div><aside aria-label="Publicidade Hotel Pulga's Pet" style="border:1px solid #ffffff35;border-radius:22px;padding:18px;background:#ffffff08;text-align:center"><div class="eye light" style="color:#D49A23">PUBLICIDADE</div><div aria-hidden="true" style="font-size:42px;margin:10px 0">🐾</div><strong style="display:block;color:#fff;font-size:20px">Hotel Pulga's Pet</strong><p style="color:#d7deea;font-size:13px;line-height:1.5">Hospedagem e cuidado para seu pet.</p><a href="https://www.instagram.com/hotel.pulgaspet/" target="_blank" rel="noreferrer nofollow sponsored" aria-label="Abrir Instagram oficial do Hotel Pulga's Pet em nova aba" style="display:inline-flex;align-items:center;justify-content:center;gap:9px;width:100%;margin-top:8px;border:1px solid #D49A23;border-radius:12px;padding:11px 14px;color:#fff;font-weight:800;font-size:13px"><span aria-hidden="true">◎</span> Acessar Instagram</a></aside></div></footer><nav class="mobile"><a href="/">Início</a><a href="/#ultimas">Últimas</a><a href="/reels">Vídeos</a><button onclick="document.getElementById('search')?.focus()">Buscar</button><button onclick="pauta(true)">Pauta</button></nav>${pautaModal()}`;
 send(res,layout('Início',content));
}

async function article(res,slug){
 const a=await db.articleBySlug(slug);if(!a)return send(res,layout('Não encontrada',`${header()}<main class="empty"><h1>Matéria não encontrada</h1><a class="btn" href="/">Voltar</a></main>`),404);
 const canonical=`https://empautaavare.com.br/materia/${a.slug}`;
 const content=`${header()}<main><div class="article"><div class="eye">${esc(a.category)}</div><h1>${esc(a.title)}</h1><p class="deck">${esc(a.deck)}</p><div class="meta"><span>${esc(a.author)}</span><span>•</span><time>${fmt(a.published_at||a.created_at)}</time><span>•</span><span>Atualizada ${fmt(a.updated_at)}</span></div><div class="share"><button onclick="share('${canonical}','${attr(a.title)}')">Compartilhar</button><a target="_blank" rel="noreferrer" href="https://wa.me/?text=${encodeURIComponent(a.title+' '+canonical)}">WhatsApp</a><button onclick="navigator.clipboard.writeText('${canonical}');alert('Link copiado')">Copiar link</button></div></div>${a.cover_url?`<div class="heroimg"><img src="${attr(a.cover_url)}" alt="${attr(a.cover_alt||a.title)}"></div>`:''}<div class="article">${a.video_url?`<video controls playsinline style="width:100%;max-height:80vh;background:#000;border-radius:20px" src="${attr(a.video_url)}"></video>`:''}<div class="body">${paras(a.body)}</div>${a.source_label?`<aside class="source"><strong>Fonte e contexto documental</strong><p>${a.source_url?`<a target="_blank" rel="noreferrer" href="${attr(a.source_url)}">${esc(a.source_label)}</a>`:esc(a.source_label)}</p><small>O Em Pauta identifica a origem principal utilizada na apuração quando ela está disponível.</small></aside>`:''}</div></main>${pautaModal()}`;
 send(res,layout(a.title,content));
}

async function reels(res){const rows=(await db.published()).filter(a=>a.video_url);if(!rows.length)return send(res,layout('Em Vídeo',`${header()}<main class="empty"><div class="eye">EM PAUTA REELS</div><h1>Em Vídeo</h1><p>Os próximos vídeos reais publicados pelo painel aparecerão aqui.</p><a class="btn" href="/">Voltar</a></main>`));send(res,layout('Em Vídeo',`<a class="btn" style="position:fixed;z-index:5;top:15px;left:15px" href="/">← Voltar</a><main class="reelFeed">${rows.map(a=>`<section class="reel"><div><video controls playsinline poster="${attr(a.cover_url)}" src="${attr(a.video_url)}"></video><h2 style="color:#fff">${esc(a.title)}</h2><a href="/materia/${attr(a.slug)}">Leia a matéria →</a></div></section>`).join('')}</main>`))}

function login(res,msg=''){send(res,layout('Login da redação',`<main class="login"><section class="loginBox"><img src="/logo.svg" alt="Em Pauta"><h1>Painel editorial</h1><p>Entre para criar, revisar e publicar matérias.</p>${msg?`<p class="notice">${esc(msg)}</p>`:''}<form class="form" method="post" action="/action/login"><input type="email" name="email" required placeholder="E-mail"><input type="password" name="password" required placeholder="Senha"><button class="btn">Entrar</button></form></section></main>`))}

async function admin(req,res){
 if(!isAdmin(req))return redirect(res,'/admin/login');const rows=await db.allArticles(),subs=await db.submissions(),id=query(req).get('edit'),e=rows.find(x=>x.id===id)||{};
 const list=rows.map(a=>`<div class="item"><strong>${esc(a.title)}</strong><small>${esc(a.category)} • ${a.status==='published'?'Publicado':'Rascunho'}${a.featured?' • Destaque':''}${a.breaking?' • Plantão':''}</small><div class="row"><a class="ghost" href="/admin?edit=${attr(a.id)}">Editar</a>${a.status==='published'?`<a class="ghost" target="_blank" href="/materia/${attr(a.slug)}">Ver</a>`:''}<form method="post" action="/action/delete" onsubmit="return confirm('Excluir esta matéria?')"><input type="hidden" name="id" value="${attr(a.id)}"><button class="ghost">Excluir</button></form></div></div>`).join('');
 const queue=subs.map(s=>`<div class="item"><strong>${esc(s.subject)}</strong><small>${esc(s.name)} • ${fmt(s.created_at)} • ${esc(s.status)}</small><p>${esc(s.message)}</p>${s.media_url?`<a target="_blank" href="${attr(s.media_url)}">Abrir mídia</a>`:''}<form class="row" method="post" action="/action/submission-status"><input type="hidden" name="id" value="${attr(s.id)}"><button class="ghost" name="status" value="Em checagem">Em checagem</button><button class="ghost" name="status" value="Arquivada">Arquivar</button></form></div>`).join('');
 const opts=categories.map(c=>`<option ${e.category===c?'selected':''}>${c}</option>`).join('');
 const content=`<header class="top"><div class="shell nav"><a href="/"><img class="logo" src="/logo.svg" alt="Em Pauta"></a><div class="actions" style="margin-left:auto"><a class="ghost" target="_blank" href="/">Ver portal</a><form method="post" action="/action/logout"><button class="ghost">Sair</button></form></div></div></header><main class="shell adminMain"><div class="adminGrid"><section class="panel"><div class="eye">CMS EDITORIAL</div><h1>${id?'Editar matéria':'Nova matéria'}</h1><p class="notice">Rascunho e publicação ficam separados. A fonte acompanha a matéria e a pauta do leitor permanece em triagem.</p><form class="editor" method="post" action="/action/save"><input type="hidden" name="id" value="${attr(e.id||'')}"><label class="full">Título<input name="title" required maxlength="160" value="${attr(e.title||'')}"></label><label class="full">Subtítulo<input name="deck" maxlength="300" value="${attr(e.deck||'')}"></label><label>Editoria<select name="category">${opts}</select></label><label>Autoria<input name="author" value="${attr(e.author||'Redação Em Pauta')}"></label><label class="full">Texto<textarea name="body" required>${esc(e.body||'')}</textarea></label><label class="full">Imagem de capa / URL<input name="cover_url" value="${attr(e.cover_url||'')}"></label><label class="full">Descrição acessível da imagem<input name="cover_alt" value="${attr(e.cover_alt||'')}"></label><label class="full">Vídeo / Reel (URL)<input name="video_url" value="${attr(e.video_url||'')}"></label><label class="full">Fonte / crédito documental<input name="source_label" value="${attr(e.source_label||'')}"></label><label class="full">Link da fonte<input name="source_url" value="${attr(e.source_url||'')}"></label><label>Status<select name="status"><option value="draft" ${e.status==='draft'?'selected':''}>Rascunho</option><option value="published" ${e.status==='published'?'selected':''}>Publicado</option></select></label><label>Exibição<div class="row"><label><input type="checkbox" name="featured" ${e.featured?'checked':''}> Destaque</label><label><input type="checkbox" name="breaking" ${e.breaking?'checked':''}> Plantão</label></div></label><div class="full row"><a class="ghost" href="/admin">Limpar</a><button class="btn">Salvar</button></div></form><h2>Matérias</h2>${list}</section><aside><section class="panel"><div class="eye">TRIAGEM</div><h2>Pautas recebidas</h2>${queue||'<p>Nenhuma pauta recebida.</p>'}</section></aside></div></main>`;send(res,layout('Painel da redação',content,true));
}

async function action(req,res,name){const b=await form(req);if(name==='login'){if(b.email===process.env.ADMIN_EMAIL&&b.password===process.env.ADMIN_PASSWORD&&process.env.SESSION_SECRET){res.setHeader('Set-Cookie',`ep_session=${encodeURIComponent(token(b.email))}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=43200`);return redirect(res,'/admin')}return login(res,'Login ou senha inválidos.')}if(name==='logout'){res.setHeader('Set-Cookie','ep_session=; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=0');return redirect(res,'/admin/login')}if(!isAdmin(req))return redirect(res,'/admin/login');if(name==='save'){if(b.title)await db.saveArticle(b);return redirect(res,'/admin')}if(name==='delete'){if(b.id)await db.deleteArticle(b.id);return redirect(res,'/admin')}if(name==='submission-status'){if(b.id&&['Em checagem','Arquivada'].includes(b.status))await db.setSubmissionStatus(b.id,b.status);return redirect(res,'/admin')}return redirect(res,'/admin')}
async function pauta(req,res){const b=await form(req);if(!b.website&&b.name&&b.subject&&b.message)await db.createSubmission(b);return redirect(res,'/?pauta=enviada')}

module.exports=async(req,res)=>{try{const q=query(req),view=q.get('view')||'';if(req.method==='POST'&&view==='action')return action(req,res,q.get('name'));if(req.method==='POST'&&view==='pauta')return pauta(req,res);if(view==='article')return article(res,q.get('slug'));if(view==='reels')return reels(res);if(view==='admin')return admin(req,res);if(view==='login')return isAdmin(req)?redirect(res,'/admin'):login(res);return home(req,res)}catch(e){console.error(e);return send(res,layout('Erro',`${header()}<main class="empty"><h1>Não foi possível carregar esta página.</h1><p>O erro foi registrado para revisão.</p><a class="btn" href="/">Voltar</a></main>`),500)}};
